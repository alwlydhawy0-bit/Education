/**
 * Header — STRUCTURAL SHELL ONLY.
 *
 * Task 002 builds the greeting, the search field, the notification bell and the
 * avatar. The one decision already made: it is a `<header>` inside the main
 * column, not above the sidebar, which is what the reference design shows and
 * what keeps the sidebar full-height.
 */
export default function Header() {
  return (
    <header className="flex h-16 shrink-0 items-center gap-4 border-b border-accent-subtle bg-surface px-6">
      {/* Task 002: ترحيب بالمستخدم + حقل البحث + الإشعارات + الصورة الشخصية */}
    </header>
  );
}
