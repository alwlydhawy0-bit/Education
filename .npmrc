# Security: do not run arbitrary lifecycle scripts from transitive dependencies.
# Packages that genuinely need a build step must be allow-listed explicitly.
enable-pre-post-scripts=false
auto-install-peers=true
strict-peer-dependencies=false
resolution-mode=highest
